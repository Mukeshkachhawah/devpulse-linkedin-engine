# CS Fundamentals — 2-Minute Revision

Timer: 2 minutes. No notes first.

## Minute 1 — Lightning explains

**OS (10s)** process/thread + VM  
**Net (10s)** TCP vs UDP + HTTPS  
**DB (10s)** B+Tree + ACID  
**Mem (10s)** cache line + vector vs list  
**Compiler (10s)** compile vs link + UB  
**Linux (10s)** pipe + SIGTERM  
**Concurrency (10s)** race + mutex  

## Minute 2 — Rapid fire

1. Dirty read? → read uncommitted.  
2. Three-way handshake ends with? → client ACK.  
3. Why B+Tree for disk? → high fanout / fewer I/Os / ranges.  
4. `volatile` for threads in C++? → No.  
5. Cache line typical size order? → tens of bytes (often 64).  
6. Undefined reference error stage? → link.  
7. `chmod 755` meaning gist? → owner rwx; others rx.  
8. CV wait needs? → mutex + predicate.  
9. Thrashing? → paging dominates.  
10. Latency vs bandwidth? → delay vs throughput.

## If failed ≥2

Tonight: only the failed topic lesson + its interview Q section.
